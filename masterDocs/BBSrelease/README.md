# BBS release notes

Each release post contains only the matching release HIGHLIGHTS copied from
`..\scoWorkList.txt`. Keep earlier release documents when adding a new version.

Current local release: `v0.9release.txt`.
