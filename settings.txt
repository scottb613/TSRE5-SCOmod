// TSRE GenX v0.6 settings
// Both # and // can be used for comments.
// Recommended public uiScale range: 1.00 to 1.25. Scott's 32" 2K setup uses 1.15.


//// General / System

consoleOutput = false             // display log output in realtime in the command window
debugOutput = false               // enable extended logging detail
fullscreen = false                // start maximized/fullscreen
soundEnabled = false              // legacy TSRE sound support
scoSoundEnabled = true            // GenX editor UI sounds: click, buzz, chirp
startapp = r                      // r = Route Editor, c = Consist Editor, s = Shape Viewer
systemTheme = false               // true uses your Windows palette
unsafemode = false                // only enable for risky/advanced features
useImperial = on                  // convert some display values from metric
usenNumPad = true
useWorkingDir = false             // false saves logs to the TSRE folder
warningBox = true                 // warn before exiting without saving


//// Logging

logfiledays = 20                  // delete logs older than X days
logfilemax = 50000                // keep only X logs


//// Startup / Route Selection

// gameRoot = ""                  // your MSTS/ORTS "Train Simulator" folder
// routeName = ""                 // route name to skip route selection menu
createNewIfNotExist = true        // create routeName if it does not already exist
// startTileX = ""                // optional start location
// startTileY = ""                // optional start location

geoPath = e:/SRTM                 // drive/folder containing HGT files
loadActivities = true             // check route activities for errors
loadAllWFiles = false             // true loads the entire route for error checking; slower on large routes

// routeMergeString = ""          // merge route offsets, e.g. "IRM:0:0:0"
// routeMergeTDB = false          // set true to merge TDBs
// routeMergeTerrain = false      // set true to overwrite overlapping terrain heights
// routeMergeTerrtex = false      // set true to overwrite overlapping terrain textures


//// UI / Windows / Scaling

mainWindowLayout = "PWTC"         // window order: P = Properties, T = Tools, W = World, C = Control Panel
toolsHidden = false               // only show the viewport
uiScale = 1.15                    // global editor UI font/panel scale; 1.00 to 1.25 recommended


//// Camera / Viewport

cameraFov = 35
cameraSpeedMax = 40               // camera movement with SHIFT
cameraSpeedMin = 1                // camera movement with CTRL
cameraSpeedStd = 3                // camera movement normal
cameraStickToTerrain = true       // stop camera from going underground, toggled with "/" key
lockCamera = false                // same as pressing "." while moving the camera
mouseSpeed = 0.1                  // mouse look/pan speed


//// Rendering / Performance

AASamples = 0
allowObjLag = 10
imageSubstitution = true          // allow ACE or DDS substitution if one is missing
imageUpgrade = true               // prefer DDS if available; false uses shape-defined texture only
maxObjLag = 10
MSTSshadows = false               // simplified shadows when true
newSymbols = true                 // true uses newer TSRE symbols; false uses older pyramids
objectLod = 3000                  // 2000 is plenty for most routes
railProfile = 0.7175, 0.7895      // rail edges for dynamic track display
flexDebugWindow = false           // show the old Flex solver debug window
shadowLowMapSize = 1024
shadowMapSize = 2048
shadowsEnabled = false            // affects performance if true
textureQuality = 1
tileLod = 1                       // tiles in each direction to load
useQuadTree = true


//// View Overlays / Markers

fogColor =
fogDensity =
hudEnabled = false
hudScale = 1                      // HUD text scale
markerHeight = 10                 // height of marker sticks
markerLines = true                // show marker lines when route loads
markerText = 2.5                  // marker text size
oglDefaultLineWidth = 1           // width of standard lines
renderTrItems = false             // show black TrItem markers
sectionLineHeight = 5.0           // grey section line height
selectedColor = #B612FF           // object selection line color
selectedWidth = 2                 // object selection line width
selectedTerrColor = #FFB612       // terrain selection line color
selectedTerrWidth = 4             // terrain selection line width
skyColor = #E0FFFF
useSuperelevation = false         // apply superelevation when rendering curves
viewCompass = false               // show compass at top center
viewMarkers = true                // view markers selected in Control Panel
wireLineHeight = 6.8              // yellow TDB/RDB line height


//// Object Editing / Placement

defaultElevationBox = 0
defaultMoveStep = 0.25
ignoreMissingGlobalShapes = true  // false shows only track/road shapes present in Global\Shapes
leaveTrackShapeAfterDelete = false // use only when deleting track/road but keeping TDB lines
maxAutoPlacement = 999            // max distance in meters for auto-placement
numRecentItems = 30               // length of recently used items list
sigOffset = 2.5                   // offset for signal object placement
snapableRadius = 20               // max distance to snap to nearest object
snapableOnlyRot = false           // false allows free rotation
trackElevationMaxPm = 100         // maximum grade in permille
useOnlyPositiveQuaternions = false
useTdbEmptyItems = true           // preserve node numbering when deleting TDB items
writeEnabled = true               // set false for read-only
writeTDB = true                   // set false to manually add to TDB via Z key


//// Terrain Editing / F2 Brush Defaults

terrainBrushColor = "#1B2E29"
terrainBrushIntensity = 70
terrainBrushSize = 25
terrainCut = 2
terrainEmbankment = 2
terrainRadius = 9
terrainSize = 1


//// Terrain Texture / Seasonal Paint

preloadTextures = "rock.ace"      // supports ace, bmp, dds, png files in TERRTEX folder


//// Map / F3 Imagery

mapImageResolution = 4096         // downloaded map imagery resolution
// mapengine =                    // optional map imagery provider selector

// MapBox:
// imageMapsUrl = https://api.mapbox.com/styles/v1/mapbox/satellite-v9/static/{lon},{lat},{zoom}/{res}x{res}?access_token=
// imageMapsZoomOffset = -1       // required for MapBox
// MapAPIKey = {your API key}

// Google Maps:
// imageMapsUrl = http://maps.googleapis.com/maps/api/staticmap?center={lat},{lon}&zoom={zoom}&size={res}x{res}&maptype=satellite&key=
// MapAPIKey = {your API key}


//// Route File Cleanup / Maintenance

autoFix = false                   // repair TDB anomalies
deepunderground = -100            // flag pieces that are not on terrain
deleteTrWatermarks = true         // remove detail not used by ORTS
deleteViewDbSpheres = true        // remove detail not used by ORTS
legacySupport = false             // retain ViewDBSphere and VDBID when true
listfiles = true                  // create lists of files used/unused on exit
objectsToRemove = ""              // requires listfiles and loadAllWFiles; comma-separated shapes
routeRebuildTDB = true            // requires unsafemode
sortTileObjects = true            // order items by detail level on save


//// Advanced / Network / Multi-User

# fpsLimit = 59
# playerMode =
# proceduralTracks = true
# serverAuth = yes
# serverLogin = yes@yes.com
# useNetworkEng = false


//// Consist Editor

ceWindowLayout = "cu1"            // C = Consists, 1 = Main List, 2 = Second List
colorConView = "#a2a2a2"
colorShapeView = "#a2a2a2"
includeFolder = "openrails"       // optional override for OpenRailsCZSK Project
loadConsists = true               // set false to skip loading consists
ortsEngEnable = true              // give precedence to settings in /OpenRails folders


//// Legacy / Disabled

# season = "Spring"               // disabled in GenX; use the F2 Texture Set selector
# seasonalEditing = on            // disabled in GenX; use the F2 Texture Set selector
